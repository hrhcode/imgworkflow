server:
  port: 8080

spring:
  application:
    name: imgworkflow-backend
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB

file:
  upload:
    path: ./uploads

logging:
  level:
    com.imgworkflow: debug
    org.springframework: info
